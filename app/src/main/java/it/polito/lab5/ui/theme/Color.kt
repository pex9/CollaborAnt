package it.polito.lab5.ui.theme

import androidx.compose.ui.graphics.Color

object CollaborantColors{
    // COLLABORANT MAIN PALETTE
    val Yellow = Color(0xFFFBA834)
    val Yellow40 = Yellow.copy(alpha = 0.4f)
    val DarkBlue = Color(0XFF333A73)
    val DarkBlue40 = DarkBlue.copy(alpha = 0.4f)
    val MediumBlue = Color(0XFF387ADF)
    val MediumBlue40 = MediumBlue.copy(alpha = 0.4f)
    val LightBlue = Color(0XFF50C4ED)
    val LightBlue40 = LightBlue.copy(alpha = 0.4f)

    // BACKGROUND COLOURS
    val PageBackGroundGray = Color(0XFFF8F8F8)
    val PageBackGroundGray2 = Color(0xFFF2F2F2)
    val CardBackGroundGray = Color(0XFFE0E0E0)

    // tag
    val PriorityRed = Color(0XFFEB5757)
    val PriorityOrange2 = Color(0xFFFF8C00)
    val PriorityOrange = Color(0XFFF2C94C)
    val PriorityGreen = Color(0XFF27AE60)
    val NoPriorityGray = Color(0XFF4F4F4F)

    // OTHERS
    val BorderGray = Color(0XFF828282)
}